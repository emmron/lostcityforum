{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx astro build",
  "installCommand": "npm install",
  "framework": "astro",
  "functions": {
    "api/**/*": {
      "memory": 1024
    }
  }
}